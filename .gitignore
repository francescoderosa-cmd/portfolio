# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Git installer left in this folder — do not commit it
Git/
Git-*.exe

# source files for images already converted to webp (keep locally, don't commit)
assets/**/*.pdf
assets/**/*.png
assets/*.zip

# raw video and source photos dropped into people-portal — keep local, NEVER publish
# (only the optimised pp-*.webp are served)
assets/**/*.mp4
assets/**/*.mov
assets/people-portal/*.jpg
assets/people-portal/*.jpeg

# working material dropped into assets, never publish archives
assets/*.zip

# private-sector project source folders (multi-GB design sources, confidential
# client docs, .sketch/.psd/.ai/.key/.docx/.jpg) — keep local, NEVER commit.
# Only the optimised webp visuals in assets/legacy/ are published.
assets/Prada/
assets/Miu Miu/
assets/Tim/
assets/CMV - Citomegalovirus/
